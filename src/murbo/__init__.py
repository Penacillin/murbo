"""Murbo — murder-mystery × Sudoku puzzle pipeline.

PNG → extract (LLM vision) → structured JSON → solve (constraint solver) →
baked puzzle → native web UI plays & checks.
"""

__version__ = "0.1.0"
